# cardano-p2p-dapp
minimal-footprint Cardano-native dApp where wallet addresses = user identities, a project-specific native token powers P2P commerce (items + services), and governance stays in the hands of token holders. No bloated React/Next.js stack, no heavy backend, no unnecessary JS. We'll treat the blockchain as the source of truth for ownership
